# Optional: GitHub personal access token for higher rate limits and private repo access
# Create a token at https://github.com/settings/tokens (classic PAT with repo scope)
# Fine-grained tokens with "Contents: Read" permission also work
GITHUB_TOKEN=ghp_xxxxx